# SmolVLA Policy Training Configuration
#
# Usage:
#   physicalai fit --config configs/physicalai/smolvla/pusht/default.yaml

model:
  class_path: physicalai.policies.SmolVLA
  init_args:
    # Optional camera remapping for pretrained SmolVLA checkpoints.
    # Example: map dataset camera keys to pretrained camera slot indices.
    # image_key_reorder_map:
    #   overview: 0
    #   gripper: 1
    image_key_reorder_map: {}
    # Total number of camera slots; unfilled slots become masked dummy cameras.
    # Values <= 0 keep only the cameras present in the batch.
    num_cameras: 0
  # SnapFlow self-distillation (arxiv.org/abs/2604.05656)
  # Uncomment to enable 1-step inference via progressive self-distillation:
  # snapflow_enabled: true
  # snapflow_alpha: 0.5
  # snapflow_lambda: 1.0
  # snapflow_num_inference_steps: 1

data:
  class_path: physicalai.data.lerobot.LeRobotDataModule
  init_args:
    repo_id: "lerobot/pusht"
    train_batch_size: 64
    data_format: "physicalai"
    val_gym:
      class_path: physicalai.gyms.pusht.PushTGym
    image_transforms:
      class_path: torchvision.transforms.v2.Compose
      init_args:
        transforms:
          - class_path: torchvision.transforms.v2.ColorJitter
            init_args:
              contrast:
                - 0.8
                - 1.2
              brightness:
                - 0.8
                - 1.2

trainer:
  max_epochs: 100
  accelerator: gpu
  devices: 1
  precision: 32
  log_every_n_steps: 100
  check_val_every_n_epoch: 10
