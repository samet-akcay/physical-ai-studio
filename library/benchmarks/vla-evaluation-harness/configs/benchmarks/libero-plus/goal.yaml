server:
  url: ws://localhost:8000

docker:
  image: ghcr.io/worv-ai/vla-evaluation-harness-public/libero-plus:latest

output_dir: ./results/pi05-pytorch-libero-plus-goal

benchmarks:
  - benchmark: vla_eval.benchmarks.libero_plus.benchmark:LIBEROPlusBenchmark
    subname: libero_plus_goal
    episodes_per_task: 1
    params:
      suite: libero_goal
      seed: 7
      num_steps_wait: 10
      send_state: true
      send_wrist_image: true
