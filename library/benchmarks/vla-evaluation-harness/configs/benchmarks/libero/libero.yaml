# All standard LIBERO evaluation suites (40 tasks, 50 episodes per task).
server:
  url: ws://localhost:8000

docker:
  image: ghcr.io/allenai/vla-evaluation-harness/libero:latest

output_dir: ./results

benchmarks:
  - benchmark: vla_eval.benchmarks.libero.benchmark:LIBEROBenchmark
    subname: libero_spatial
    episodes_per_task: 50
    params:
      suite: libero_spatial
      seed: 7
      num_steps_wait: 10

  - benchmark: vla_eval.benchmarks.libero.benchmark:LIBEROBenchmark
    subname: libero_object
    episodes_per_task: 50
    params:
      suite: libero_object
      seed: 7
      num_steps_wait: 10

  - benchmark: vla_eval.benchmarks.libero.benchmark:LIBEROBenchmark
    subname: libero_goal
    episodes_per_task: 50
    params:
      suite: libero_goal
      seed: 7
      num_steps_wait: 10

  - benchmark: vla_eval.benchmarks.libero.benchmark:LIBEROBenchmark
    subname: libero_10
    episodes_per_task: 50
    params:
      suite: libero_10
      seed: 7
      num_steps_wait: 10
